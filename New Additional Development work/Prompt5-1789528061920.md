OBJECTIVE:
Write the production-ready code for the i3 Sandboxed Execution Service and MCP server layer that `i3-code` interacts with on the cluster.

REQUIREMENTS:
1. Container Sandbox Execution:
   - Create a Kubernetes Job / Pod template for ephemeral CodeAct Python execution on OpenShift 4.17.
   - Scope resources via NetworkPolicy (deny egress except to cluster internal services), runAsNonRoot, and mount temporary workspace volumes (`emptyDir` backed by memory or Ceph).
2. MCP Gateway Service (FastAPI / TypeScript):
   - Implement an MCP server exposing core cluster capabilities to `i3-code`:
     * `read_db_schema`: Queries Crunchy PostgreSQL PGBouncer metadata securely.
     * `execute_sandboxed_code`: Runs Python/Node scripts in the ephemeral sandbox pod.
     * `query_curriculum_vector`: Semantic search across ChromaDB collections.
     * `vault_get_secret`: Fetches designated development credentials from OpenBao.
3. Granite Guardian Release Path:
   - Enforce a middleware pipe where any code or terminal output produced by the agent is classified by Granite Guardian before being displayed or executed.

DELIVERABLE:
Provide the complete Python/FastAPI implementation for the MCP Server, the Kubernetes pod deployment YAML with OpenShift SecurityContextConstraints (SCC), and sample MCP tool declarations.