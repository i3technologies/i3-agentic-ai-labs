OBJECTIVE:
Generate an end-to-end, live interactive demonstration script showcasing `i3-code` (the Agentic Runtime) running on the i3 Sovereign AI Appliance (i3-SAA) or Cloud Platform.

DEMONSTRATION SCENARIO:
A solutions engineer sits down at an empty terminal, installs the CLI, authenticates against sovereign infrastructure, and asks the agent to build a new agentic coworker service for the Severnside Institute of Technology (SIT) campus:

1. Initialization & Connect:
   - Developer runs `npm install -g @i3/code && i3-code login --server https://api.smartlab.i3technologies.co.ke`.
   - Browser pop-up authenticates via Keycloak SSO.
2. Context Ingestion:
   - The CLI loads the local repository, detects `_context/about-me.md` and reads the SIT Technical Implementation Guide from SeaweedFS S3.
3. Plan Generation:
   - Developer prompt: "Inspect our Crunchy Postgres cluster, verify the sit_db schema, and scaffold the FastAPI `sit-registrar-agent` microservice with LiteLLM integration."
   - `i3-code` enters `Plan` mode, showing an execution tree with required MCP tool calls, database tables accessed, and generated files.
4. Human Approval & Autonomous Execution:
   - Developer approves the plan.
   - The agent switches to `Agent` mode, launches an ephemeral sandbox on ROKS, writes the code, runs test suites, queries ChromaDB for sample course data, and generates a Git commit.
5. Evidence & Audit:
   - Show how the entire transaction generated a Langfuse trace ID, passed Granite Guardian safety checks, and logged an immutable event to the platform audit log.

DELIVERABLE:
Provide the terminal dialogue script (including realistic CLI ANSI logs, ASCII progress bars, diff displays, and interactive approval prompts) to use in executive briefings and technical workshops.