apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: talent-default-deny
  namespace: i3-talent
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: talent-allow-ingress
  namespace: i3-talent
spec:
  podSelector: {}
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: openshift-ingress
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-sit
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-ar
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-monitoring
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: talent-allow-egress
  namespace: i3-talent
spec:
  podSelector: {}
  policyTypes: [Egress]
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-model-gateway
      ports: [{port: 4000, protocol: TCP}]
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-data
      ports: [{port: 5432, protocol: TCP}]
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-messaging
      ports: [{port: 9092, protocol: TCP}]
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-ai-lab
      ports: [{port: 8000, protocol: TCP}]
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-auth
      ports: [{port: 8443, protocol: TCP}]
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: i3-ott
      ports: [{port: 8333, protocol: TCP}]
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
