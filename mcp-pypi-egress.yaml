apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-pypi-egress
  namespace: i3-ar
spec:
  podSelector:
    matchLabels:
      app: mcp-gateway
  policyTypes:
  - Egress
  egress:
  - ports:
    - port: 443
      protocol: TCP
    - port: 80
      protocol: TCP
